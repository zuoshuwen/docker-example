###########################################################
###################### General Setup ######################
###########################################################


### Drivers ################################################

# All volumes driver
VOLUMES_DRIVER=local

# All Networks driver
NETWORKS_DRIVER=bridge

### Docker compose files ##################################

# Select which docker-compose files to include. If using docker-sync append `:docker-compose.sync.yml` at the end
COMPOSE_FILE=docker-compose.yml

# Define the prefix of container names. This is useful if you have multiple projects that use laradock to have seperate containers per project.
COMPOSE_PROJECT_NAME=docker-example

### MYSQL Master #################################################
MYSQL_MASTER_VERSION=5.7
MYSQL_MASTER_DATABASE=default
MYSQL_MASTER_PORT=3307
MYSQL_MASTER_ROOT_PASSWORD=root

### MYSQL Slave #################################################
MYSQL_SLAVE_VERSION=5.7
MYSQL_SLAVE_DATABASE=default
MYSQL_SLAVE_PORT=3308
MYSQL_SLAVE_ROOT_PASSWORD=root


WORKSPACE_TIMEZONE=UTC